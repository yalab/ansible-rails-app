---
- hosts: all
  user: root
  vars_files:
    - vars/casework.yml

  roles:
    - common
    - secure_sshd
    - { role: users, tags: users }
    - ruby
    - postgresql
    - deploy
    - webserver
    - casework

  handlers: 
    - name: Restart puma
      action: service name=puma state=restarted